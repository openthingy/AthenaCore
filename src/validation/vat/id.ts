class vatId {
    public static viesValidation(vatId: string): string {
        // Vies is down lol
        return vatId;
    }

    // Returns whether the Vat ID is a personal or business
    public static vatIdType(vatId: string): string {
        
        return vatId;
    }
}

export { vatId };